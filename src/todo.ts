/**
 * Placeholder body for the M2 stubs.
 *
 * @module
 * Accepts the stub's parameters so `noUnusedParameters` stays satisfied until
 * the real body replaces the call. Every `todo(...)` left in `src/` is one
 * rep still to do. Delete this file when the last call is gone.
 */
export const todo = (..._inputs: unknown[]): never => {
  throw new Error("not implemented");
};
